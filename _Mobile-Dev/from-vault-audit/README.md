# Скиллы найденные в проектных папках

> **Источник:** `Projects/Focus Buddi – ADHD Planner/AppDev/App Dev Studio/.claude/skills/`
> **Дата аудита:** 22 мая 2026
>
> В обеих папках (Focus Buddi и Sipper) — **идентичная коллекция App Dev Studio**. Копировал из Focus Buddi, оригиналы в обоих проектах не тронуты.

---

## Что скопировано (37 скиллов + 1 .skill архив)

### ios-dev/ — 10 нативных iOS скиллов

| Скилл | Назначение |
|---|---|
| `app-clips` | iOS App Clips (lightweight приложения через QR / NFC) |
| `app-icon-optimization` | Оптимизация иконки приложения |
| `appstore-screens` | Генерация App Store скриншотов (с `screenshot-specs.yaml`) |
| `arch-check` | Architecture check для iOS-проекта |
| `figma-ios-design-pipeline` | Figma → iOS дизайн-пайплайн (с references и scripts) |
| `in-app-events` | In-App Events (App Store discoverability feature) |
| `stripe-integration-expert` | Stripe для iOS |
| `svg-animations` | SVG анимации на iOS |
| `test-gaps` | Покрытие тестами — выявление пробелов |
| `tokens-sync` | Design tokens sync (с `mapping.yaml`) |

### product-monetization/ — 12 скиллов под mobile-монетизацию

`paywall-optimization`, `paywall-upgrade-cro`, `subscription-lifecycle`, `retention-optimization`, `churn-prevention`, `onboarding-optimization`, `onboarding-cro`, `pricing-strategy`, `monetization-strategy`, `saas-metrics-coach`, `ab-test-setup`, `position-me`

### marketing-mobile/ — 8 mobile-специфичных маркетинговых

`apple-search-ads`, `ua-campaign` (User Acquisition), `attribution-setup` (mobile attribution), `web-to-app-funnel`, `meta-ads-skill`, `review-management`, `launch-strategy`, `press-and-pr`

### quality/ — 7 quality/UI скиллов

`taste-check`, `audit-ui`, `stop-slop`, `impeccable`, `ship-gate`, `i18n-translator`, `localization`

### Отдельно

- `figma-ios-design-pipeline.skill` — zip-архив скилла из корня Focus Buddi

---

## Что НЕ скопировано (умышленно)

Из категории `marketing/` пропустил web/контент-маркетинговые скиллы, не специфичные для mobile:

- `blog-cover-image-cli` — для блогов
- `cook-the-blog` — для блогов
- `copy-lint` — общий копирайтинг
- `email-marketing-bible-main` — email маркетинг (есть в основном vault)
- `google-trends-api-skills` — общая аналитика трендов
- `landing-page-generator` — лендинги (есть в new-plugins)
- `pr-review-expert` — PR review для кода

Из `quality/`:

- `office-hours` — workflow-скилл, не mobile-специфичный
- `gdpr-dsgvo-expert` — уже есть в new-plugins (compliance-regulatory)
- `investigate` — общий debugging

Если что-то из них нужно — путь к оригиналу:
`Projects/Focus Buddi – ADHD Planner/AppDev/App Dev Studio/.claude/skills/`

---

## Полезный контекст

В корне `Projects/Focus Buddi – ADHD Planner/SKILLS.md` лежит MASTER Skills Map проекта — какой скилл вызывать для какой задачи Focus Buddi. Это **карта применения** скиллов на конкретный проект, рекомендую посмотреть как пример организации.
