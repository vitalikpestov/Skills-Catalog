# Semantic Tests

`test/semantic/` is the home for deterministic correctness fixtures and permanent regressions.

Today the active semantic suite still runs through `test/smoke.mjs`; this directory establishes the Stage 5 structure so future curated fixtures can move out of the monolithic smoke file without changing the taxonomy again.
