// ESLint Flat Config Template for TypeScript Projects
// Generated by ln-741-linter-configurator
// Format: eslint.config.ts (requires jiti as dev dependency for Node.js < 22.10)
// Docs: https://eslint.org/docs/latest/use/configure/configuration-files
//
// NOTE: typescript-eslint v8.57+ provides defineConfig() as replacement for tseslint.config().
// If on v8.57+, use: import { defineConfig } from "typescript-eslint";
// If on v8.56 or earlier, use: import tseslint from "typescript-eslint"; + tseslint.config()

import eslint from "@eslint/js";
import tseslint from "typescript-eslint";
import eslintConfigPrettier from "eslint-config-prettier/flat";
import unicorn from "eslint-plugin-unicorn";

// OPTIONAL: Uncomment for React projects
// import react from "eslint-plugin-react";
// import reactHooks from "eslint-plugin-react-hooks";

export default tseslint.config(
  // Global ignores
  {
    ignores: [
      "dist/",
      "build/",
      "node_modules/",
      "coverage/",
      // CUSTOMIZE: add project-specific ignores
    ],
  },

  // Base recommended rules
  eslint.configs.recommended,

  // TypeScript recommended (type-checked) + stylistic
  // For stricter rules, replace with: strictTypeChecked + stylisticTypeChecked
  ...tseslint.configs.recommendedTypeChecked,
  ...tseslint.configs.stylisticTypeChecked,

  // TypeScript parser config with projectService (modern approach, replaces project: true)
  {
    languageOptions: {
      parserOptions: {
        projectService: {
          // CUSTOMIZE: list config files that need type checking
          allowDefaultProject: ["eslint.config.ts"],
        },
        tsconfigRootDir: import.meta.dirname,
      },
    },
  },

  // Unicorn: modern JavaScript/TypeScript patterns (134+ rules)
  // NOTE: use unicorn.configs.recommended (flat/recommended is unsupported)
  unicorn.configs.recommended,

  // Custom rules
  {
    rules: {
      // Console: warn but allow error/warn channels
      "no-console": ["warn", { allow: ["warn", "error"] }],

      // Unused vars: allow underscore-prefixed (matches Ruff F841)
      "@typescript-eslint/no-unused-vars": [
        "error",
        { argsIgnorePattern: "^_", varsIgnorePattern: "^_" },
      ],

      // Allow numbers/booleans in template literals (common pattern)
      "@typescript-eslint/restrict-template-expressions": [
        "error",
        { allowNumber: true, allowBoolean: true },
      ],

      // Unicorn overrides: disable opinionated rules that conflict with common patterns
      // CUSTOMIZE: adjust based on your project needs
      "unicorn/prevent-abbreviations": "off",
      "unicorn/no-null": "off",
    },
  },

  // Test files: disable type-checked rules (tests often excluded from tsconfig)
  {
    files: ["**/*.test.ts", "**/*.test.tsx", "**/*.spec.ts", "tests/**/*.ts"],
    ...tseslint.configs.disableTypeChecked,
    rules: {
      ...tseslint.configs.disableTypeChecked.rules,
      "@typescript-eslint/no-explicit-any": "off",
      "@typescript-eslint/no-unsafe-assignment": "off",
      "@typescript-eslint/no-unsafe-member-access": "off",
      "@typescript-eslint/no-unsafe-call": "off",
      "@typescript-eslint/no-unsafe-argument": "off",
      "no-console": "off",
    },
  },

  // OPTIONAL: Uncomment for React projects
  // {
  //   plugins: { react, "react-hooks": reactHooks },
  //   rules: {
  //     "react-hooks/rules-of-hooks": "error",
  //     "react-hooks/exhaustive-deps": "warn",
  //   },
  //   settings: { react: { version: "detect" } },
  // },

  // Prettier MUST be last (disables formatting rules that conflict with Prettier)
  eslintConfigPrettier
);
