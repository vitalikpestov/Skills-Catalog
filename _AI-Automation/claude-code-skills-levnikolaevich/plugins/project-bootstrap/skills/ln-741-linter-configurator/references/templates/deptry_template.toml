# Deptry Configuration Template
# Generated by ln-741-linter-configurator
# Detects unused, missing, and transitive dependency issues.
# Run: uv run deptry .
# Docs: https://deptry.com/

[tool.deptry]
# CUSTOMIZE: add directories to exclude from scanning
extend_exclude = ["scripts", ".claude", "tests"]

# CUSTOMIZE: map packages with different install vs import names
# [tool.deptry.package_module_name_map]
# python-docx = "docx"

# CUSTOMIZE: add per-rule ignores for false positives
# [tool.deptry.per_rule_ignores]
# DEP002 = ["alembic"]        # CLI tool, not imported
# DEP003 = ["starlette"]      # Transitive from FastAPI
