// lint-staged Configuration Template
// Generated by ln-742-precommit-setup
// Location: .lintstagedrc.mjs or lint-staged.config.mjs

export default {
  // TypeScript/JavaScript files: lint and format
  '*.{ts,tsx,js,jsx}': ['eslint --fix', 'prettier --write'],

  // Style files: format only
  '*.{css,scss,less}': ['prettier --write'],

  // Data/config files: format only
  '*.{json,yaml,yml}': ['prettier --write'],

  // Markdown: format only
  '*.md': ['prettier --write'],

  // NOTE: For .NET files in mixed projects:
  // WRONG: '*.cs': ['dotnet format --include']
  // RIGHT: Run dotnet format separately or use:
  // '*.cs': ['dotnet format whitespace --include']
};
