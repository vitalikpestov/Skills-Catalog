# conftest.py Template for FastAPI/async Python Projects
# Generated by ln-743-test-infrastructure
# Location: tests/conftest.py
#
# Pattern source: https://fastapi.tiangolo.com/advanced/async-tests/
#
# NOTE: AsyncClient does NOT trigger FastAPI lifespan events (startup/shutdown).
# If your app relies on lifespan, add asgi-lifespan:
#   uv add --dev asgi-lifespan
# Then wrap: async with LifespanManager(app) as manager:
#   transport = ASGITransport(app=manager.app)
# See: https://github.com/florimondmanca/asgi-lifespan

"""Shared test fixtures for the test suite."""

from collections.abc import AsyncGenerator

import pytest
from httpx import ASGITransport, AsyncClient

# CUSTOMIZE: change to your FastAPI app import path
from app.main import app


@pytest.fixture
async def client() -> AsyncGenerator[AsyncClient, None]:
    """Async HTTP client bound to FastAPI app.

    Uses ASGITransport (httpx 0.28+) instead of unsupported app= kwarg.
    No real network calls -- all requests go through the ASGI interface.
    """
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        yield ac
